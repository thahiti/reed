# MDViewer Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 마크다운 문서를 아름답게 렌더링하는 읽기 전용 Electron 데스크톱 뷰어를 구축한다.

**Architecture:** Electron (Main + Preload + Renderer) 3-프로세스 구조. Renderer는 React로 UI를 구성하고, unified/remark/rehype 파이프라인으로 마크다운을 React 컴포넌트 트리로 변환한다. 테마는 TypeScript 객체 → CSS Variables로 런타임 적용한다.

**Tech Stack:** Electron 33+, React 19, TypeScript (strict), electron-vite, unified/remark/rehype, KaTeX, Mermaid, highlight.js, fuse.js, electron-store, Vitest, Playwright

---

## File Structure

```
src/
├── main/
│   ├── main.ts                  # 앱 엔트리, BrowserWindow, open-file 이벤트
│   ├── menu.ts                  # 앱 메뉴 (File, View, Window)
│   └── ipc/
│       ├── fileHandlers.ts      # file:read, file:open-dialog, file:resolve-path, file:open-external
│       ├── historyHandlers.ts   # history:get, history:add (electron-store)
│       └── themeHandlers.ts     # theme:get-system, theme:on-change (nativeTheme)
├── preload/
│   └── preload.ts               # contextBridge API 노출
├── renderer/
│   ├── index.html
│   ├── index.tsx                # React 엔트리
│   ├── App.tsx                  # 탭 + 테마 + 글로벌 키보드
│   ├── components/
│   │   ├── TabBar.tsx           # 탭 바 UI
│   │   ├── MarkdownView.tsx     # 마크다운 렌더링 컨테이너
│   │   ├── QuickOpen.tsx        # Cmd+P 퍼지 검색 모달
│   │   ├── Welcome.tsx          # 빈 상태 화면
│   │   └── markdown/
│   │       ├── Heading.tsx
│   │       ├── Paragraph.tsx
│   │       ├── CodeBlock.tsx
│   │       ├── InlineCode.tsx
│   │       ├── Table.tsx
│   │       ├── Blockquote.tsx
│   │       ├── List.tsx
│   │       ├── Image.tsx
│   │       ├── Link.tsx
│   │       ├── Divider.tsx
│   │       ├── Checkbox.tsx
│   │       └── MermaidDiagram.tsx
│   ├── hooks/
│   │   ├── useMarkdown.ts       # unified 파이프라인 훅
│   │   ├── useTabs.ts           # 탭 상태 관리
│   │   ├── useTheme.ts          # 테마 + 시스템 연동
│   │   └── useQuickOpen.ts      # 퍼지 검색 로직
│   ├── themes/
│   │   ├── types.ts             # Theme 타입
│   │   ├── light.ts             # 라이트 테마
│   │   ├── dark.ts              # 다크 테마
│   │   └── applyTheme.ts        # CSS Variables 적용
│   ├── pipeline/
│   │   └── createProcessor.ts   # unified 파이프라인 조립
│   └── styles/
│       ├── global.css           # 기본 스타일
│       └── fonts.css            # @font-face
└── shared/
    └── types.ts                 # IPC 채널 타입, HistoryEntry, Tab
```

---

## Task 1: Electron + Vite + React 스캐폴드

**Files:**
- Create: `package.json`, `electron.vite.config.ts`, `tsconfig.json`, `tsconfig.node.json`, `tsconfig.web.json`
- Create: `src/main/main.ts`, `src/preload/preload.ts`, `src/renderer/index.html`, `src/renderer/index.tsx`

- [ ] **Step 1: electron-vite 프로젝트 초기화**

```bash
cd /Users/randy/side_project/mdviewer
pnpm create @quick-start/electron . --template react-ts
```

프롬프트가 나오면:
- Project name: mdviewer
- Framework: React
- TypeScript: Yes

- [ ] **Step 2: 기존 하네스 파일 충돌 확인 및 복구**

electron-vite가 기존 파일을 덮어쓸 수 있으므로 확인:

```bash
git diff --stat
```

`.eslintrc.cjs`, `.prettierrc` 등 기존 하네스 파일이 변경되었다면 복구:

```bash
git checkout -- .eslintrc.cjs .prettierrc CLAUDE.md
```

- [ ] **Step 3: tsconfig.json strict 설정 확인**

`tsconfig.json`에 다음이 있는지 확인하고, 없으면 추가:

```json
{
  "compilerOptions": {
    "strict": true,
    "noUncheckedIndexedAccess": true
  }
}
```

- [ ] **Step 4: 앱 실행 확인**

```bash
pnpm install
pnpm dev
```

빈 BrowserWindow가 열리면 성공. `Ctrl+C`로 종료.

- [ ] **Step 5: 테스트/린트 의존성 설치**

```bash
pnpm add -D vitest @testing-library/react @testing-library/jest-dom jsdom eslint-plugin-functional eslint-config-prettier
```

- [ ] **Step 6: Vitest 설정**

`vitest.config.ts` 생성:

```typescript
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: ['./tests/setup.ts'],
  },
});
```

`tests/setup.ts` 생성:

```typescript
import '@testing-library/jest-dom/vitest';
```

- [ ] **Step 7: package.json 스크립트 확인**

`package.json`에 다음 스크립트가 있는지 확인:

```json
{
  "scripts": {
    "dev": "electron-vite dev",
    "build": "electron-vite build",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint . --ext .ts,.tsx",
    "lint:fix": "eslint . --ext .ts,.tsx --fix"
  }
}
```

- [ ] **Step 8: pnpm test 실행 확인**

```bash
pnpm test
```

테스트가 없으므로 에러 없이 종료되면 성공.

- [ ] **Step 9: 커밋**

```bash
git add -A
git commit -m "chore: scaffold electron-vite react-ts project

electron-vite + React + TypeScript 기본 프로젝트 구조 생성.
Vitest, ESLint, Prettier 설정 포함."
```

---

## Task 2: Shared 타입 정의 + Preload + 보안 설정

**Files:**
- Create: `src/shared/types.ts`
- Modify: `src/preload/preload.ts`
- Modify: `src/main/main.ts`
- Test: `tests/shared/types.test.ts`

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/shared/types.test.ts`:

```typescript
import { describe, it, expectTypeOf } from 'vitest';
import type { IpcChannels, HistoryEntry, Tab, TabState } from '../src/shared/types';

describe('Shared Types', () => {
  it('IpcChannels should have file:read channel', () => {
    expectTypeOf<IpcChannels['file:read']['args']>().toEqualTypeOf<readonly [filePath: string]>();
    expectTypeOf<IpcChannels['file:read']['return']>().toEqualTypeOf<string>();
  });

  it('HistoryEntry should have required fields', () => {
    expectTypeOf<HistoryEntry>().toHaveProperty('filePath');
    expectTypeOf<HistoryEntry>().toHaveProperty('fileName');
    expectTypeOf<HistoryEntry>().toHaveProperty('openedAt');
  });

  it('Tab should have required fields', () => {
    expectTypeOf<Tab>().toHaveProperty('id');
    expectTypeOf<Tab>().toHaveProperty('filePath');
    expectTypeOf<Tab>().toHaveProperty('fileName');
    expectTypeOf<Tab>().toHaveProperty('content');
  });

  it('TabState should have tabs and activeTabId', () => {
    expectTypeOf<TabState>().toHaveProperty('tabs');
    expectTypeOf<TabState>().toHaveProperty('activeTabId');
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
pnpm test -- tests/shared/types.test.ts
```

Expected: FAIL — `../src/shared/types` 모듈 없음.

- [ ] **Step 3: 타입 정의 구현**

`src/shared/types.ts`:

```typescript
export type IpcChannels = {
  'file:read': { args: readonly [filePath: string]; return: string };
  'file:open-dialog': { args: readonly []; return: string | null };
  'file:resolve-path': {
    args: readonly [basePath: string, relativePath: string];
    return: string;
  };
  'file:open-external': { args: readonly [url: string]; return: void };
  'history:get': { args: readonly []; return: ReadonlyArray<HistoryEntry> };
  'history:add': { args: readonly [filePath: string]; return: void };
  'theme:get-system': { args: readonly []; return: 'light' | 'dark' };
};

export type HistoryEntry = {
  readonly filePath: string;
  readonly fileName: string;
  readonly openedAt: string;
};

export type Tab = {
  readonly id: string;
  readonly filePath: string;
  readonly fileName: string;
  readonly content: string;
};

export type TabState = {
  readonly tabs: ReadonlyArray<Tab>;
  readonly activeTabId: string | null;
};
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
pnpm test -- tests/shared/types.test.ts
```

Expected: PASS

- [ ] **Step 5: preload.ts 구현**

`src/preload/preload.ts`:

```typescript
import { contextBridge, ipcRenderer } from 'electron';
import type { IpcChannels } from '../shared/types';

const api = {
  invoke: <K extends keyof IpcChannels>(
    channel: K,
    ...args: IpcChannels[K]['args']
  ): Promise<IpcChannels[K]['return']> =>
    ipcRenderer.invoke(channel, ...args),

  on: (channel: string, callback: (...args: readonly unknown[]) => void) => {
    const subscription = (_event: Electron.IpcRendererEvent, ...args: readonly unknown[]) =>
      callback(...args);
    ipcRenderer.on(channel, subscription);
    return () => {
      ipcRenderer.removeListener(channel, subscription);
    };
  },
};

contextBridge.exposeInMainWorld('api', api);

export type ElectronApi = typeof api;
```

- [ ] **Step 6: main.ts 보안 설정**

`src/main/main.ts`의 BrowserWindow 생성 부분에 보안 설정 확인/수정:

```typescript
const mainWindow = new BrowserWindow({
  width: 900,
  height: 670,
  webPreferences: {
    preload: join(__dirname, '../preload/preload.js'),
    contextIsolation: true,
    nodeIntegration: false,
    sandbox: true,
  },
});
```

- [ ] **Step 7: pnpm dev로 앱 실행 확인**

```bash
pnpm dev
```

BrowserWindow가 열리고 콘솔에 에러 없으면 성공.

- [ ] **Step 8: 커밋**

```bash
git add src/shared/types.ts src/preload/preload.ts src/main/main.ts tests/shared/types.test.ts
git commit -m "feat(shared): add IPC channel types and secure preload

IpcChannels, HistoryEntry, Tab, TabState 타입 정의.
contextBridge로 typed API 노출, 보안 설정 적용."
```

---

## Task 3: 테마 시스템

**Files:**
- Create: `src/renderer/themes/types.ts`
- Create: `src/renderer/themes/light.ts`
- Create: `src/renderer/themes/dark.ts`
- Create: `src/renderer/themes/applyTheme.ts`
- Test: `tests/renderer/themes/applyTheme.test.ts`

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/renderer/themes/applyTheme.test.ts`:

```typescript
import { describe, it, expect, beforeEach } from 'vitest';
import { applyTheme } from '../../../src/renderer/themes/applyTheme';
import { lightTheme } from '../../../src/renderer/themes/light';
import { darkTheme } from '../../../src/renderer/themes/dark';

describe('applyTheme', () => {
  beforeEach(() => {
    document.documentElement.style.cssText = '';
  });

  it('should set CSS variables from light theme', () => {
    applyTheme(lightTheme);
    const style = document.documentElement.style;
    expect(style.getPropertyValue('--color-bg')).toBe(lightTheme.colors.bg);
    expect(style.getPropertyValue('--color-text')).toBe(lightTheme.colors.text);
    expect(style.getPropertyValue('--font-body')).toBe(lightTheme.fonts.body);
  });

  it('should set CSS variables from dark theme', () => {
    applyTheme(darkTheme);
    const style = document.documentElement.style;
    expect(style.getPropertyValue('--color-bg')).toBe(darkTheme.colors.bg);
    expect(style.getPropertyValue('--color-text')).toBe(darkTheme.colors.text);
  });

  it('should set heading scale variables', () => {
    applyTheme(lightTheme);
    const style = document.documentElement.style;
    expect(style.getPropertyValue('--heading-h1')).toBe(lightTheme.headingScale.h1);
    expect(style.getPropertyValue('--heading-h6')).toBe(lightTheme.headingScale.h6);
  });

  it('should override previous theme when called again', () => {
    applyTheme(lightTheme);
    applyTheme(darkTheme);
    const style = document.documentElement.style;
    expect(style.getPropertyValue('--color-bg')).toBe(darkTheme.colors.bg);
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
pnpm test -- tests/renderer/themes/applyTheme.test.ts
```

Expected: FAIL

- [ ] **Step 3: Theme 타입 구현**

`src/renderer/themes/types.ts`:

```typescript
export type Theme = {
  readonly name: string;
  readonly fonts: {
    readonly body: string;
    readonly code: string;
    readonly bodySize: string;
    readonly codeSize: string;
    readonly lineHeight: string;
    readonly codeLineHeight: string;
  };
  readonly colors: {
    readonly bg: string;
    readonly text: string;
    readonly textSecondary: string;
    readonly heading: string;
    readonly link: string;
    readonly linkHover: string;
    readonly codeBg: string;
    readonly codeText: string;
    readonly blockquoteBg: string;
    readonly blockquoteBorder: string;
    readonly blockquoteText: string;
    readonly tableBorder: string;
    readonly tableHeaderBg: string;
    readonly tableStripeBg: string;
    readonly divider: string;
    readonly selection: string;
  };
  readonly spacing: {
    readonly paragraph: string;
    readonly headingMarginTop: string;
    readonly headingMarginBottom: string;
    readonly contentMaxWidth: string;
    readonly contentPadding: string;
  };
  readonly headingScale: {
    readonly h1: string;
    readonly h2: string;
    readonly h3: string;
    readonly h4: string;
    readonly h5: string;
    readonly h6: string;
  };
};
```

- [ ] **Step 4: 라이트 테마 구현**

`src/renderer/themes/light.ts`:

```typescript
import type { Theme } from './types';

export const lightTheme: Theme = {
  name: 'light',
  fonts: {
    body: 'Inter, Pretendard, -apple-system, system-ui, sans-serif',
    code: "'JetBrains Mono', 'SF Mono', Menlo, monospace",
    bodySize: '16px',
    codeSize: '14px',
    lineHeight: '1.7',
    codeLineHeight: '1.5',
  },
  colors: {
    bg: '#ffffff',
    text: '#24292f',
    textSecondary: '#656d76',
    heading: '#1f2328',
    link: '#0969da',
    linkHover: '#0550ae',
    codeBg: '#f6f8fa',
    codeText: '#24292f',
    blockquoteBg: 'transparent',
    blockquoteBorder: '#d0d7de',
    blockquoteText: '#656d76',
    tableBorder: '#d0d7de',
    tableHeaderBg: '#f6f8fa',
    tableStripeBg: '#f6f8fa',
    divider: '#d8dee4',
    selection: '#ddf4ff',
  },
  spacing: {
    paragraph: '16px',
    headingMarginTop: '24px',
    headingMarginBottom: '16px',
    contentMaxWidth: '780px',
    contentPadding: '32px',
  },
  headingScale: {
    h1: '2em',
    h2: '1.5em',
    h3: '1.25em',
    h4: '1em',
    h5: '0.875em',
    h6: '0.85em',
  },
};
```

- [ ] **Step 5: 다크 테마 구현**

`src/renderer/themes/dark.ts`:

```typescript
import type { Theme } from './types';

export const darkTheme: Theme = {
  name: 'dark',
  fonts: {
    body: 'Inter, Pretendard, -apple-system, system-ui, sans-serif',
    code: "'JetBrains Mono', 'SF Mono', Menlo, monospace",
    bodySize: '16px',
    codeSize: '14px',
    lineHeight: '1.7',
    codeLineHeight: '1.5',
  },
  colors: {
    bg: '#0d1117',
    text: '#e6edf3',
    textSecondary: '#8b949e',
    heading: '#f0f6fc',
    link: '#58a6ff',
    linkHover: '#79c0ff',
    codeBg: '#161b22',
    codeText: '#e6edf3',
    blockquoteBg: 'transparent',
    blockquoteBorder: '#3b434b',
    blockquoteText: '#8b949e',
    tableBorder: '#30363d',
    tableHeaderBg: '#161b22',
    tableStripeBg: '#161b22',
    divider: '#21262d',
    selection: '#264f78',
  },
  spacing: {
    paragraph: '16px',
    headingMarginTop: '24px',
    headingMarginBottom: '16px',
    contentMaxWidth: '780px',
    contentPadding: '32px',
  },
  headingScale: {
    h1: '2em',
    h2: '1.5em',
    h3: '1.25em',
    h4: '1em',
    h5: '0.875em',
    h6: '0.85em',
  },
};
```

- [ ] **Step 6: applyTheme 구현**

`src/renderer/themes/applyTheme.ts`:

```typescript
import type { Theme } from './types';

export const applyTheme = (theme: Theme): void => {
  const style = document.documentElement.style;

  // Fonts
  style.setProperty('--font-body', theme.fonts.body);
  style.setProperty('--font-code', theme.fonts.code);
  style.setProperty('--font-body-size', theme.fonts.bodySize);
  style.setProperty('--font-code-size', theme.fonts.codeSize);
  style.setProperty('--line-height', theme.fonts.lineHeight);
  style.setProperty('--code-line-height', theme.fonts.codeLineHeight);

  // Colors
  Object.entries(theme.colors).forEach(([key, value]) => {
    const cssKey = key.replace(/([A-Z])/g, '-$1').toLowerCase();
    style.setProperty(`--color-${cssKey}`, value);
  });

  // Spacing
  Object.entries(theme.spacing).forEach(([key, value]) => {
    const cssKey = key.replace(/([A-Z])/g, '-$1').toLowerCase();
    style.setProperty(`--spacing-${cssKey}`, value);
  });

  // Heading scale
  Object.entries(theme.headingScale).forEach(([key, value]) => {
    style.setProperty(`--heading-${key}`, value);
  });
};
```

- [ ] **Step 7: 테스트 통과 확인**

```bash
pnpm test -- tests/renderer/themes/applyTheme.test.ts
```

Expected: PASS

- [ ] **Step 8: 커밋**

```bash
git add src/renderer/themes/ tests/renderer/themes/
git commit -m "feat(theme): add theme system with light and dark themes

Theme 타입 정의, 라이트/다크 테마 값, applyTheme으로 CSS Variables 적용.
테마 속성 변경만으로 전체 UI 일괄 반영 가능."
```

---

## Task 4: 폰트 번들링 + 글로벌 CSS

**Files:**
- Create: `src/renderer/styles/fonts.css`
- Create: `src/renderer/styles/global.css`

- [ ] **Step 1: 폰트 패키지 설치**

```bash
pnpm add @fontsource/inter @fontsource/jetbrains-mono pretendard
```

- [ ] **Step 2: fonts.css 작성**

`src/renderer/styles/fonts.css`:

```css
@import '@fontsource/inter/latin-400.css';
@import '@fontsource/inter/latin-500.css';
@import '@fontsource/inter/latin-600.css';
@import '@fontsource/inter/latin-700.css';
@import '@fontsource/jetbrains-mono/latin-400.css';
@import '@fontsource/jetbrains-mono/latin-700.css';
@import 'pretendard/dist/web/static/pretendard.min.css';
```

- [ ] **Step 3: global.css 작성**

`src/renderer/styles/global.css`:

```css
*,
*::before,
*::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html {
  font-size: var(--font-body-size, 16px);
}

body {
  font-family: var(--font-body);
  color: var(--color-text);
  background-color: var(--color-bg);
  line-height: var(--line-height);
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

::selection {
  background-color: var(--color-selection);
}

::-webkit-scrollbar {
  width: 8px;
}

::-webkit-scrollbar-track {
  background: transparent;
}

::-webkit-scrollbar-thumb {
  background: var(--color-divider);
  border-radius: 4px;
}
```

- [ ] **Step 4: index.tsx에서 CSS import**

`src/renderer/index.tsx` 상단에 추가:

```typescript
import './styles/fonts.css';
import './styles/global.css';
```

- [ ] **Step 5: pnpm dev로 폰트 적용 확인**

```bash
pnpm dev
```

앱에서 텍스트가 Inter/Pretendard 폰트로 렌더링되면 성공.

- [ ] **Step 6: 커밋**

```bash
git add src/renderer/styles/ src/renderer/index.tsx package.json pnpm-lock.yaml
git commit -m "feat(fonts): bundle Inter, Pretendard, JetBrains Mono

woff2 폰트를 앱에 번들하여 오프라인에서도 동작.
글로벌 CSS에서 CSS Variables로 폰트 참조."
```

---

## Task 5: 테마 IPC + useTheme 훅

**Files:**
- Create: `src/main/ipc/themeHandlers.ts`
- Create: `src/renderer/hooks/useTheme.ts`
- Test: `tests/renderer/hooks/useTheme.test.ts`

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/renderer/hooks/useTheme.test.ts`:

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { renderHook, act } from '@testing-library/react';
import { useTheme } from '../../../src/renderer/hooks/useTheme';
import { lightTheme } from '../../../src/renderer/themes/light';
import { darkTheme } from '../../../src/renderer/themes/dark';

const mockInvoke = vi.fn();
const mockOn = vi.fn(() => vi.fn());

vi.stubGlobal('window', {
  ...window,
  api: { invoke: mockInvoke, on: mockOn },
  matchMedia: vi.fn(() => ({
    matches: false,
    addEventListener: vi.fn(),
    removeEventListener: vi.fn(),
  })),
});

describe('useTheme', () => {
  beforeEach(() => {
    vi.clearAllMocks();
    mockInvoke.mockResolvedValue('light');
  });

  it('should return light theme by default', () => {
    const { result } = renderHook(() => useTheme());
    expect(result.current.theme).toEqual(lightTheme);
  });

  it('should return dark theme when system is dark', async () => {
    mockInvoke.mockResolvedValue('dark');
    const { result } = renderHook(() => useTheme());
    await act(async () => {});
    expect(result.current.theme).toEqual(darkTheme);
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
pnpm test -- tests/renderer/hooks/useTheme.test.ts
```

Expected: FAIL

- [ ] **Step 3: themeHandlers.ts 구현**

`src/main/ipc/themeHandlers.ts`:

```typescript
import { ipcMain, nativeTheme, BrowserWindow } from 'electron';

export const registerThemeHandlers = (): void => {
  ipcMain.handle('theme:get-system', () =>
    nativeTheme.shouldUseDarkColors ? 'dark' : 'light',
  );

  nativeTheme.on('updated', () => {
    const theme = nativeTheme.shouldUseDarkColors ? 'dark' : 'light';
    BrowserWindow.getAllWindows().forEach((win) => {
      win.webContents.send('theme:on-change', theme);
    });
  });
};
```

- [ ] **Step 4: useTheme 훅 구현**

`src/renderer/hooks/useTheme.ts`:

```typescript
import { useState, useEffect } from 'react';
import type { Theme } from '../themes/types';
import { lightTheme } from '../themes/light';
import { darkTheme } from '../themes/dark';
import { applyTheme } from '../themes/applyTheme';

const themeMap = { light: lightTheme, dark: darkTheme } as const;

export const useTheme = () => {
  const [theme, setTheme] = useState<Theme>(lightTheme);

  useEffect(() => {
    window.api.invoke('theme:get-system').then((mode) => {
      const resolved = themeMap[mode];
      setTheme(resolved);
      applyTheme(resolved);
    });

    const unsubscribe = window.api.on('theme:on-change', (mode: unknown) => {
      if (mode === 'light' || mode === 'dark') {
        const resolved = themeMap[mode];
        setTheme(resolved);
        applyTheme(resolved);
      }
    });

    return unsubscribe;
  }, []);

  return { theme };
};
```

- [ ] **Step 5: main.ts에서 themeHandlers 등록**

`src/main/main.ts`의 `app.whenReady()` 안에 추가:

```typescript
import { registerThemeHandlers } from './ipc/themeHandlers';

app.whenReady().then(() => {
  registerThemeHandlers();
  // ... BrowserWindow 생성
});
```

- [ ] **Step 6: 테스트 통과 확인**

```bash
pnpm test -- tests/renderer/hooks/useTheme.test.ts
```

Expected: PASS

- [ ] **Step 7: 커밋**

```bash
git add src/main/ipc/themeHandlers.ts src/renderer/hooks/useTheme.ts src/main/main.ts tests/renderer/hooks/useTheme.test.ts
git commit -m "feat(theme): add system theme detection and auto-switch

nativeTheme 기반 시스템 다크/라이트 모드 감지.
실시간 테마 변경 시 IPC로 renderer에 알림."
```

---

## Task 6: 파일 읽기 IPC

**Files:**
- Create: `src/main/ipc/fileHandlers.ts`
- Test: `tests/main/ipc/fileHandlers.test.ts`

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/main/ipc/fileHandlers.test.ts`:

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { readFileContent, resolveRelativePath } from '../../../src/main/ipc/fileHandlers';
import { readFile } from 'node:fs/promises';
import { resolve, dirname } from 'node:path';

vi.mock('node:fs/promises');

describe('fileHandlers', () => {
  beforeEach(() => {
    vi.clearAllMocks();
  });

  describe('readFileContent', () => {
    it('should read file as utf-8 string', async () => {
      vi.mocked(readFile).mockResolvedValue('# Hello');
      const result = await readFileContent('/path/to/file.md');
      expect(result).toBe('# Hello');
      expect(readFile).toHaveBeenCalledWith('/path/to/file.md', 'utf-8');
    });
  });

  describe('resolveRelativePath', () => {
    it('should resolve relative path from base file', () => {
      const result = resolveRelativePath('/docs/readme.md', './images/logo.png');
      expect(result).toBe(resolve('/docs', './images/logo.png'));
    });

    it('should reject directory traversal beyond base directory', () => {
      expect(() =>
        resolveRelativePath('/docs/readme.md', '../../../etc/passwd'),
      ).toThrow();
    });

    it('should allow relative path within same directory tree', () => {
      const result = resolveRelativePath('/docs/sub/readme.md', '../images/logo.png');
      expect(result).toBe(resolve('/docs/sub', '../images/logo.png'));
    });
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
pnpm test -- tests/main/ipc/fileHandlers.test.ts
```

Expected: FAIL

- [ ] **Step 3: fileHandlers 구현**

`src/main/ipc/fileHandlers.ts`:

```typescript
import { ipcMain, dialog, shell, BrowserWindow } from 'electron';
import { readFile } from 'node:fs/promises';
import { resolve, dirname } from 'node:path';

export const readFileContent = async (filePath: string): Promise<string> =>
  readFile(filePath, 'utf-8');

export const resolveRelativePath = (basePath: string, relativePath: string): string => {
  const baseDir = dirname(basePath);
  const resolved = resolve(baseDir, relativePath);

  if (!resolved.startsWith(baseDir)) {
    throw new Error(`Path traversal detected: ${relativePath}`);
  }

  return resolved;
};

export const registerFileHandlers = (): void => {
  ipcMain.handle('file:read', (_event, filePath: string) =>
    readFileContent(filePath),
  );

  ipcMain.handle('file:open-dialog', async () => {
    const focusedWindow = BrowserWindow.getFocusedWindow();
    const result = await dialog.showOpenDialog(focusedWindow!, {
      properties: ['openFile'],
      filters: [{ name: 'Markdown', extensions: ['md', 'markdown'] }],
    });
    return result.canceled ? null : result.filePaths[0] ?? null;
  });

  ipcMain.handle(
    'file:resolve-path',
    (_event, basePath: string, relativePath: string) =>
      resolveRelativePath(basePath, relativePath),
  );

  ipcMain.handle('file:open-external', (_event, url: string) =>
    shell.openExternal(url),
  );
};
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
pnpm test -- tests/main/ipc/fileHandlers.test.ts
```

Expected: PASS

- [ ] **Step 5: main.ts에서 등록**

`src/main/main.ts`에 추가:

```typescript
import { registerFileHandlers } from './ipc/fileHandlers';

app.whenReady().then(() => {
  registerThemeHandlers();
  registerFileHandlers();
  // ...
});
```

- [ ] **Step 6: 커밋**

```bash
git add src/main/ipc/fileHandlers.ts tests/main/ipc/fileHandlers.test.ts src/main/main.ts
git commit -m "feat(ipc): add file read, open dialog, and path resolution

파일 읽기, 파일 선택 다이얼로그, 상대경로 해석 IPC 핸들러.
디렉토리 탈출 공격 차단 경로 검증 포함."
```

---

## Task 7: 마크다운 렌더링 파이프라인 코어

**Files:**
- Create: `src/renderer/pipeline/createProcessor.ts`
- Create: `src/renderer/hooks/useMarkdown.ts`
- Test: `tests/renderer/pipeline/createProcessor.test.ts`

- [ ] **Step 1: 마크다운 의존성 설치**

```bash
pnpm add unified remark-parse remark-gfm remark-math remark-rehype rehype-katex rehype-highlight rehype-react
pnpm add katex
```

- [ ] **Step 2: 실패하는 테스트 작성**

`tests/renderer/pipeline/createProcessor.test.ts`:

```typescript
import { describe, it, expect } from 'vitest';
import { processMarkdown } from '../../../src/renderer/pipeline/createProcessor';

describe('createProcessor', () => {
  it('should convert heading to React element', () => {
    const result = processMarkdown('# Hello');
    expect(result).toBeDefined();
  });

  it('should convert paragraph to React element', () => {
    const result = processMarkdown('Hello world');
    expect(result).toBeDefined();
  });

  it('should handle GFM table', () => {
    const md = '| A | B |\n|---|---|\n| 1 | 2 |';
    const result = processMarkdown(md);
    expect(result).toBeDefined();
  });

  it('should handle empty string', () => {
    const result = processMarkdown('');
    expect(result).toBeDefined();
  });

  it('should handle inline code', () => {
    const result = processMarkdown('Use `const` keyword');
    expect(result).toBeDefined();
  });

  it('should handle code block with language', () => {
    const md = '```typescript\nconst x = 1;\n```';
    const result = processMarkdown(md);
    expect(result).toBeDefined();
  });
});
```

- [ ] **Step 3: 테스트 실패 확인**

```bash
pnpm test -- tests/renderer/pipeline/createProcessor.test.ts
```

Expected: FAIL

- [ ] **Step 4: createProcessor 구현**

`src/renderer/pipeline/createProcessor.ts`:

```typescript
import { unified } from 'unified';
import remarkParse from 'remark-parse';
import remarkGfm from 'remark-gfm';
import remarkMath from 'remark-math';
import remarkRehype from 'remark-rehype';
import rehypeKatex from 'rehype-katex';
import rehypeHighlight from 'rehype-highlight';
import rehypeReact from 'rehype-react';
import { createElement, Fragment, type ReactElement } from 'react';

const createProcessor = () =>
  unified()
    .use(remarkParse)
    .use(remarkGfm)
    .use(remarkMath)
    .use(remarkRehype)
    .use(rehypeKatex)
    .use(rehypeHighlight, { detect: true })
    .use(rehypeReact, {
      createElement,
      Fragment,
    });

export const processMarkdown = (markdown: string): ReactElement => {
  const processor = createProcessor();
  const file = processor.processSync(markdown);
  return file.result as ReactElement;
};
```

- [ ] **Step 5: 테스트 통과 확인**

```bash
pnpm test -- tests/renderer/pipeline/createProcessor.test.ts
```

Expected: PASS

- [ ] **Step 6: useMarkdown 훅 구현**

`src/renderer/hooks/useMarkdown.ts`:

```typescript
import { useMemo } from 'react';
import type { ReactElement } from 'react';
import { processMarkdown } from '../pipeline/createProcessor';

export const useMarkdown = (content: string): ReactElement =>
  useMemo(() => processMarkdown(content), [content]);
```

- [ ] **Step 7: 커밋**

```bash
git add src/renderer/pipeline/ src/renderer/hooks/useMarkdown.ts tests/renderer/pipeline/
git commit -m "feat(pipeline): add unified markdown-to-react pipeline

remark-parse → remark-gfm → remark-math → remark-rehype →
rehype-katex → rehype-highlight → rehype-react 파이프라인 조립.
useMarkdown 훅으로 React 컴포넌트 변환."
```

---

## Task 8: 기본 마크다운 컴포넌트

**Files:**
- Create: `src/renderer/components/markdown/Heading.tsx`
- Create: `src/renderer/components/markdown/Paragraph.tsx`
- Create: `src/renderer/components/markdown/InlineCode.tsx`
- Create: `src/renderer/components/markdown/Blockquote.tsx`
- Create: `src/renderer/components/markdown/List.tsx`
- Create: `src/renderer/components/markdown/Divider.tsx`
- Create: `src/renderer/components/markdown/Checkbox.tsx`
- Test: `tests/renderer/components/markdown/Heading.test.tsx`

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/renderer/components/markdown/Heading.test.tsx`:

```typescript
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import { Heading } from '../../../../src/renderer/components/markdown/Heading';

describe('Heading', () => {
  it('should render h1 with correct class', () => {
    render(<Heading level={1}>Title</Heading>);
    const heading = screen.getByRole('heading', { level: 1 });
    expect(heading).toHaveTextContent('Title');
    expect(heading).toHaveClass('heading', 'heading-1');
  });

  it('should render h3', () => {
    render(<Heading level={3}>Subtitle</Heading>);
    const heading = screen.getByRole('heading', { level: 3 });
    expect(heading).toHaveTextContent('Subtitle');
  });

  it('should generate anchor id from text content', () => {
    render(<Heading level={2}>Hello World</Heading>);
    const heading = screen.getByRole('heading', { level: 2 });
    expect(heading).toHaveAttribute('id', 'hello-world');
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
pnpm test -- tests/renderer/components/markdown/Heading.test.tsx
```

Expected: FAIL

- [ ] **Step 3: 컴포넌트 구현**

`src/renderer/components/markdown/Heading.tsx`:

```tsx
import type { FC, PropsWithChildren } from 'react';

type HeadingProps = PropsWithChildren<{
  readonly level: 1 | 2 | 3 | 4 | 5 | 6;
}>;

const toAnchorId = (children: React.ReactNode): string => {
  const text = typeof children === 'string' ? children : '';
  return text.toLowerCase().replace(/\s+/g, '-').replace(/[^\w-]/g, '');
};

export const Heading: FC<HeadingProps> = ({ level, children }) => {
  const Tag = `h${level}` as const;
  const id = toAnchorId(children);

  return (
    <Tag className={`heading heading-${level}`} id={id}>
      {children}
    </Tag>
  );
};
```

`src/renderer/components/markdown/Paragraph.tsx`:

```tsx
import type { FC, PropsWithChildren } from 'react';

export const Paragraph: FC<PropsWithChildren> = ({ children }) => (
  <p className="paragraph">{children}</p>
);
```

`src/renderer/components/markdown/InlineCode.tsx`:

```tsx
import type { FC, PropsWithChildren } from 'react';

export const InlineCode: FC<PropsWithChildren> = ({ children }) => (
  <code className="inline-code">{children}</code>
);
```

`src/renderer/components/markdown/Blockquote.tsx`:

```tsx
import type { FC, PropsWithChildren } from 'react';

export const Blockquote: FC<PropsWithChildren> = ({ children }) => (
  <blockquote className="blockquote">{children}</blockquote>
);
```

`src/renderer/components/markdown/List.tsx`:

```tsx
import type { FC, PropsWithChildren } from 'react';

type ListProps = PropsWithChildren<{
  readonly ordered?: boolean;
}>;

export const List: FC<ListProps> = ({ ordered, children }) => {
  const Tag = ordered ? 'ol' : 'ul';
  return <Tag className="list">{children}</Tag>;
};

export const ListItem: FC<PropsWithChildren> = ({ children }) => (
  <li className="list-item">{children}</li>
);
```

`src/renderer/components/markdown/Divider.tsx`:

```tsx
import type { FC } from 'react';

export const Divider: FC = () => <hr className="divider" />;
```

`src/renderer/components/markdown/Checkbox.tsx`:

```tsx
import type { FC } from 'react';

type CheckboxProps = {
  readonly checked: boolean;
};

export const Checkbox: FC<CheckboxProps> = ({ checked }) => (
  <input type="checkbox" checked={checked} readOnly className="checkbox" />
);
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
pnpm test -- tests/renderer/components/markdown/Heading.test.tsx
```

Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add src/renderer/components/markdown/ tests/renderer/components/markdown/
git commit -m "feat(markdown): add basic markdown components

Heading, Paragraph, InlineCode, Blockquote, List, Divider, Checkbox.
각 컴포넌트는 CSS Variables 기반 테마 적용 준비 완료."
```

---

## Task 9: 테이블 + 코드 블록 + 이미지 + 링크 컴포넌트

**Files:**
- Create: `src/renderer/components/markdown/Table.tsx`
- Create: `src/renderer/components/markdown/CodeBlock.tsx`
- Create: `src/renderer/components/markdown/Image.tsx`
- Create: `src/renderer/components/markdown/Link.tsx`
- Test: `tests/renderer/components/markdown/CodeBlock.test.tsx`
- Test: `tests/renderer/components/markdown/Link.test.tsx`

- [ ] **Step 1: 실패하는 테스트 작성 (CodeBlock)**

`tests/renderer/components/markdown/CodeBlock.test.tsx`:

```tsx
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import { CodeBlock } from '../../../../src/renderer/components/markdown/CodeBlock';

describe('CodeBlock', () => {
  it('should render code with language label', () => {
    render(<CodeBlock language="typescript">const x = 1;</CodeBlock>);
    expect(screen.getByText('typescript')).toBeInTheDocument();
    expect(screen.getByText('const x = 1;')).toBeInTheDocument();
  });

  it('should render without language label when not specified', () => {
    render(<CodeBlock>plain code</CodeBlock>);
    expect(screen.getByText('plain code')).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: 실패하는 테스트 작성 (Link)**

`tests/renderer/components/markdown/Link.test.tsx`:

```tsx
import { describe, it, expect, vi } from 'vitest';
import { render, screen, fireEvent } from '@testing-library/react';
import { Link } from '../../../../src/renderer/components/markdown/Link';

const mockInvoke = vi.fn();
vi.stubGlobal('window', {
  ...window,
  api: { invoke: mockInvoke, on: vi.fn() },
});

describe('Link', () => {
  it('should render link text', () => {
    render(<Link href="https://example.com">Example</Link>);
    expect(screen.getByText('Example')).toBeInTheDocument();
  });

  it('should call file:open-external for external URLs', () => {
    render(<Link href="https://example.com">Example</Link>);
    fireEvent.click(screen.getByText('Example'));
    expect(mockInvoke).toHaveBeenCalledWith('file:open-external', 'https://example.com');
  });

  it('should not call open-external for .md links', () => {
    render(<Link href="./other.md">Other</Link>);
    fireEvent.click(screen.getByText('Other'));
    expect(mockInvoke).not.toHaveBeenCalledWith('file:open-external', expect.anything());
  });

  it('should handle anchor links', () => {
    render(<Link href="#section">Section</Link>);
    const link = screen.getByText('Section');
    expect(link).toHaveAttribute('href', '#section');
  });
});
```

- [ ] **Step 3: 테스트 실패 확인**

```bash
pnpm test -- tests/renderer/components/markdown/CodeBlock.test.tsx tests/renderer/components/markdown/Link.test.tsx
```

Expected: FAIL

- [ ] **Step 4: 컴포넌트 구현**

`src/renderer/components/markdown/CodeBlock.tsx`:

```tsx
import type { FC, PropsWithChildren } from 'react';

type CodeBlockProps = PropsWithChildren<{
  readonly language?: string;
}>;

export const CodeBlock: FC<CodeBlockProps> = ({ language, children }) => (
  <div className="code-block-wrapper">
    {language && <span className="code-block-language">{language}</span>}
    <pre className="code-block">
      <code className={language ? `language-${language}` : undefined}>
        {children}
      </code>
    </pre>
  </div>
);
```

`src/renderer/components/markdown/Table.tsx`:

```tsx
import type { FC, PropsWithChildren } from 'react';

export const Table: FC<PropsWithChildren> = ({ children }) => (
  <div className="table-wrapper">
    <table className="table">{children}</table>
  </div>
);

export const TableHead: FC<PropsWithChildren> = ({ children }) => (
  <thead className="table-head">{children}</thead>
);

export const TableBody: FC<PropsWithChildren> = ({ children }) => (
  <tbody className="table-body">{children}</tbody>
);

export const TableRow: FC<PropsWithChildren> = ({ children }) => (
  <tr className="table-row">{children}</tr>
);

type TableCellProps = PropsWithChildren<{
  readonly isHeader?: boolean;
  readonly align?: 'left' | 'center' | 'right';
}>;

export const TableCell: FC<TableCellProps> = ({ isHeader, align, children }) => {
  const Tag = isHeader ? 'th' : 'td';
  return (
    <Tag className="table-cell" style={align ? { textAlign: align } : undefined}>
      {children}
    </Tag>
  );
};
```

`src/renderer/components/markdown/Image.tsx`:

```tsx
import type { FC } from 'react';

type ImageProps = {
  readonly src: string;
  readonly alt?: string;
};

export const Image: FC<ImageProps> = ({ src, alt }) => (
  <img className="image" src={src} alt={alt ?? ''} loading="lazy" />
);
```

`src/renderer/components/markdown/Link.tsx`:

```tsx
import type { FC, PropsWithChildren, MouseEvent } from 'react';

type LinkProps = PropsWithChildren<{
  readonly href: string;
  readonly onOpenFile?: (filePath: string) => void;
}>;

const isExternalUrl = (href: string): boolean =>
  href.startsWith('http://') || href.startsWith('https://');

const isAnchor = (href: string): boolean =>
  href.startsWith('#');

const isMarkdownLink = (href: string): boolean =>
  href.endsWith('.md') || href.includes('.md#');

export const Link: FC<LinkProps> = ({ href, onOpenFile, children }) => {
  const handleClick = (e: MouseEvent<HTMLAnchorElement>) => {
    if (isAnchor(href)) return;

    e.preventDefault();

    if (isExternalUrl(href)) {
      window.api.invoke('file:open-external', href);
    } else if (isMarkdownLink(href) && onOpenFile) {
      onOpenFile(href);
    }
  };

  return (
    <a className="link" href={href} onClick={handleClick}>
      {children}
    </a>
  );
};
```

- [ ] **Step 5: 테스트 통과 확인**

```bash
pnpm test -- tests/renderer/components/markdown/CodeBlock.test.tsx tests/renderer/components/markdown/Link.test.tsx
```

Expected: PASS

- [ ] **Step 6: 커밋**

```bash
git add src/renderer/components/markdown/Table.tsx src/renderer/components/markdown/CodeBlock.tsx src/renderer/components/markdown/Image.tsx src/renderer/components/markdown/Link.tsx tests/renderer/components/markdown/
git commit -m "feat(markdown): add Table, CodeBlock, Image, Link components

테이블 가로스크롤, 코드블록 언어 라벨, 이미지 lazy loading,
링크 내부/외부 분기 처리 구현."
```

---

## Task 10: Mermaid 다이어그램 컴포넌트

**Files:**
- Create: `src/renderer/components/markdown/MermaidDiagram.tsx`
- Test: `tests/renderer/components/markdown/MermaidDiagram.test.tsx`

- [ ] **Step 1: mermaid 설치**

```bash
pnpm add mermaid
```

- [ ] **Step 2: 실패하는 테스트 작성**

`tests/renderer/components/markdown/MermaidDiagram.test.tsx`:

```tsx
import { describe, it, expect, vi } from 'vitest';
import { render, screen, waitFor } from '@testing-library/react';
import { MermaidDiagram } from '../../../../src/renderer/components/markdown/MermaidDiagram';

vi.mock('mermaid', () => ({
  default: {
    initialize: vi.fn(),
    render: vi.fn().mockResolvedValue({ svg: '<svg>mocked</svg>' }),
  },
}));

describe('MermaidDiagram', () => {
  it('should render mermaid diagram as SVG', async () => {
    render(<MermaidDiagram chart="graph TD; A-->B;" />);
    await waitFor(() => {
      expect(screen.getByTestId('mermaid-diagram')).toBeInTheDocument();
    });
  });
});
```

- [ ] **Step 3: 테스트 실패 확인**

```bash
pnpm test -- tests/renderer/components/markdown/MermaidDiagram.test.tsx
```

Expected: FAIL

- [ ] **Step 4: MermaidDiagram 구현**

`src/renderer/components/markdown/MermaidDiagram.tsx`:

```tsx
import { type FC, useEffect, useRef, useState } from 'react';
import mermaid from 'mermaid';

type MermaidDiagramProps = {
  readonly chart: string;
};

let mermaidId = 0;

export const MermaidDiagram: FC<MermaidDiagramProps> = ({ chart }) => {
  const [svg, setSvg] = useState<string>('');
  const containerRef = useRef<HTMLDivElement>(null);

  useEffect(() => {
    mermaid.initialize({ startOnLoad: false, theme: 'default' });

    const id = `mermaid-${++mermaidId}`;
    mermaid.render(id, chart).then(({ svg: rendered }) => {
      setSvg(rendered);
    });
  }, [chart]);

  return (
    <div
      className="mermaid-diagram"
      data-testid="mermaid-diagram"
      ref={containerRef}
      dangerouslySetInnerHTML={{ __html: svg }}
    />
  );
};
```

- [ ] **Step 5: 테스트 통과 확인**

```bash
pnpm test -- tests/renderer/components/markdown/MermaidDiagram.test.tsx
```

Expected: PASS

- [ ] **Step 6: 커밋**

```bash
git add src/renderer/components/markdown/MermaidDiagram.tsx tests/renderer/components/markdown/MermaidDiagram.test.tsx package.json pnpm-lock.yaml
git commit -m "feat(markdown): add Mermaid diagram rendering

코드 블록 언어가 mermaid일 때 SVG 다이어그램으로 렌더링."
```

---

## Task 11: 파이프라인에 커스텀 컴포넌트 매핑 연결

**Files:**
- Modify: `src/renderer/pipeline/createProcessor.ts`
- Test: `tests/renderer/pipeline/createProcessor.test.ts` (추가 테스트)

- [ ] **Step 1: createProcessor에 컴포넌트 매핑 추가**

`src/renderer/pipeline/createProcessor.ts`의 `rehype-react` 옵션에 컴포넌트 매핑 추가:

```typescript
import { Heading } from '../components/markdown/Heading';
import { Paragraph } from '../components/markdown/Paragraph';
import { InlineCode } from '../components/markdown/InlineCode';
import { CodeBlock } from '../components/markdown/CodeBlock';
import { Blockquote } from '../components/markdown/Blockquote';
import { List, ListItem } from '../components/markdown/List';
import { Table, TableHead, TableBody, TableRow, TableCell } from '../components/markdown/Table';
import { Image } from '../components/markdown/Image';
import { Link } from '../components/markdown/Link';
import { Divider } from '../components/markdown/Divider';
import { Checkbox } from '../components/markdown/Checkbox';
import { MermaidDiagram } from '../components/markdown/MermaidDiagram';

// rehype-react의 components 옵션에 매핑
.use(rehypeReact, {
  createElement,
  Fragment,
  components: {
    h1: (props) => createElement(Heading, { ...props, level: 1 }),
    h2: (props) => createElement(Heading, { ...props, level: 2 }),
    h3: (props) => createElement(Heading, { ...props, level: 3 }),
    h4: (props) => createElement(Heading, { ...props, level: 4 }),
    h5: (props) => createElement(Heading, { ...props, level: 5 }),
    h6: (props) => createElement(Heading, { ...props, level: 6 }),
    p: Paragraph,
    code: InlineCode,
    pre: (props) => {
      // mermaid 코드블록 감지
      const child = props.children?.[0];
      if (child?.props?.className?.includes('language-mermaid')) {
        return createElement(MermaidDiagram, { chart: child.props.children[0] });
      }
      const language = child?.props?.className?.replace('language-', '') ?? undefined;
      return createElement(CodeBlock, { language }, child?.props?.children);
    },
    blockquote: Blockquote,
    ul: (props) => createElement(List, { ordered: false }, props.children),
    ol: (props) => createElement(List, { ordered: true }, props.children),
    li: ListItem,
    table: Table,
    thead: TableHead,
    tbody: TableBody,
    tr: TableRow,
    th: (props) => createElement(TableCell, { isHeader: true, align: props.align }, props.children),
    td: (props) => createElement(TableCell, { isHeader: false, align: props.align }, props.children),
    img: (props) => createElement(Image, { src: props.src ?? '', alt: props.alt }),
    a: (props) => createElement(Link, { href: props.href ?? '#' }, props.children),
    hr: Divider,
    input: (props) => props.type === 'checkbox'
      ? createElement(Checkbox, { checked: props.checked ?? false })
      : createElement('input', props),
  },
})
```

- [ ] **Step 2: 기존 테스트 통과 확인**

```bash
pnpm test -- tests/renderer/pipeline/createProcessor.test.ts
```

Expected: PASS

- [ ] **Step 3: 커밋**

```bash
git add src/renderer/pipeline/createProcessor.ts
git commit -m "feat(pipeline): wire custom React components into rehype pipeline

모든 마크다운 요소를 커스텀 컴포넌트로 매핑.
mermaid 코드블록 감지하여 다이어그램으로 렌더링."
```

---

## Task 12: 탭 시스템

**Files:**
- Create: `src/renderer/hooks/useTabs.ts`
- Create: `src/renderer/components/TabBar.tsx`
- Create: `src/renderer/components/MarkdownView.tsx`
- Create: `src/renderer/components/Welcome.tsx`
- Test: `tests/renderer/hooks/useTabs.test.ts`

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/renderer/hooks/useTabs.test.ts`:

```typescript
import { describe, it, expect } from 'vitest';
import { renderHook, act } from '@testing-library/react';
import { useTabs } from '../../../src/renderer/hooks/useTabs';

describe('useTabs', () => {
  it('should start with no tabs', () => {
    const { result } = renderHook(() => useTabs());
    expect(result.current.tabs).toEqual([]);
    expect(result.current.activeTabId).toBeNull();
  });

  it('should add a tab', () => {
    const { result } = renderHook(() => useTabs());
    act(() => {
      result.current.openTab('/path/file.md', 'file.md', '# Hello');
    });
    expect(result.current.tabs).toHaveLength(1);
    expect(result.current.tabs[0]?.fileName).toBe('file.md');
    expect(result.current.activeTabId).toBe(result.current.tabs[0]?.id);
  });

  it('should not duplicate tab for same file', () => {
    const { result } = renderHook(() => useTabs());
    act(() => {
      result.current.openTab('/path/file.md', 'file.md', '# Hello');
      result.current.openTab('/path/file.md', 'file.md', '# Hello');
    });
    expect(result.current.tabs).toHaveLength(1);
  });

  it('should close a tab', () => {
    const { result } = renderHook(() => useTabs());
    act(() => {
      result.current.openTab('/path/a.md', 'a.md', '# A');
      result.current.openTab('/path/b.md', 'b.md', '# B');
    });
    const tabId = result.current.tabs[0]!.id;
    act(() => {
      result.current.closeTab(tabId);
    });
    expect(result.current.tabs).toHaveLength(1);
    expect(result.current.tabs[0]?.fileName).toBe('b.md');
  });

  it('should switch active tab', () => {
    const { result } = renderHook(() => useTabs());
    act(() => {
      result.current.openTab('/path/a.md', 'a.md', '# A');
      result.current.openTab('/path/b.md', 'b.md', '# B');
    });
    const firstTabId = result.current.tabs[0]!.id;
    act(() => {
      result.current.setActiveTab(firstTabId);
    });
    expect(result.current.activeTabId).toBe(firstTabId);
  });

  it('should activate adjacent tab when closing active tab', () => {
    const { result } = renderHook(() => useTabs());
    act(() => {
      result.current.openTab('/path/a.md', 'a.md', '# A');
      result.current.openTab('/path/b.md', 'b.md', '# B');
    });
    const activeId = result.current.activeTabId!;
    act(() => {
      result.current.closeTab(activeId);
    });
    expect(result.current.activeTabId).not.toBeNull();
  });

  it('should return null activeTabId when all tabs closed', () => {
    const { result } = renderHook(() => useTabs());
    act(() => {
      result.current.openTab('/path/a.md', 'a.md', '# A');
    });
    const tabId = result.current.tabs[0]!.id;
    act(() => {
      result.current.closeTab(tabId);
    });
    expect(result.current.activeTabId).toBeNull();
    expect(result.current.tabs).toHaveLength(0);
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
pnpm test -- tests/renderer/hooks/useTabs.test.ts
```

Expected: FAIL

- [ ] **Step 3: useTabs 구현**

`src/renderer/hooks/useTabs.ts`:

```typescript
import { useState, useCallback } from 'react';
import type { Tab } from '../../shared/types';

let nextId = 0;
const generateId = (): string => `tab-${++nextId}`;

export const useTabs = () => {
  const [tabs, setTabs] = useState<ReadonlyArray<Tab>>([]);
  const [activeTabId, setActiveTabId] = useState<string | null>(null);

  const openTab = useCallback((filePath: string, fileName: string, content: string) => {
    setTabs((prev) => {
      const existing = prev.find((t) => t.filePath === filePath);
      if (existing) {
        setActiveTabId(existing.id);
        return prev;
      }
      const newTab: Tab = { id: generateId(), filePath, fileName, content };
      setActiveTabId(newTab.id);
      return [...prev, newTab];
    });
  }, []);

  const closeTab = useCallback((tabId: string) => {
    setTabs((prev) => {
      const index = prev.findIndex((t) => t.id === tabId);
      const next = prev.filter((t) => t.id !== tabId);

      setActiveTabId((currentActive) => {
        if (currentActive !== tabId) return currentActive;
        if (next.length === 0) return null;
        const newIndex = Math.min(index, next.length - 1);
        return next[newIndex]?.id ?? null;
      });

      return next;
    });
  }, []);

  const activeTab = tabs.find((t) => t.id === activeTabId) ?? null;

  return { tabs, activeTabId, activeTab, openTab, closeTab, setActiveTab: setActiveTabId };
};
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
pnpm test -- tests/renderer/hooks/useTabs.test.ts
```

Expected: PASS

- [ ] **Step 5: TabBar, MarkdownView, Welcome 컴포넌트 구현**

`src/renderer/components/TabBar.tsx`:

```tsx
import type { FC } from 'react';
import type { Tab } from '../../shared/types';

type TabBarProps = {
  readonly tabs: ReadonlyArray<Tab>;
  readonly activeTabId: string | null;
  readonly onSelect: (id: string) => void;
  readonly onClose: (id: string) => void;
};

export const TabBar: FC<TabBarProps> = ({ tabs, activeTabId, onSelect, onClose }) => (
  <div className="tab-bar">
    {tabs.map((tab) => (
      <div
        key={tab.id}
        className={`tab-item ${tab.id === activeTabId ? 'tab-active' : ''}`}
        onClick={() => onSelect(tab.id)}
      >
        <span className="tab-title">{tab.fileName}</span>
        <button
          className="tab-close"
          onClick={(e) => {
            e.stopPropagation();
            onClose(tab.id);
          }}
        >
          ×
        </button>
      </div>
    ))}
  </div>
);
```

`src/renderer/components/MarkdownView.tsx`:

```tsx
import type { FC } from 'react';
import { useMarkdown } from '../hooks/useMarkdown';

type MarkdownViewProps = {
  readonly content: string;
};

export const MarkdownView: FC<MarkdownViewProps> = ({ content }) => {
  const rendered = useMarkdown(content);

  return (
    <div className="markdown-view">
      <div className="markdown-content">{rendered}</div>
    </div>
  );
};
```

`src/renderer/components/Welcome.tsx`:

```tsx
import type { FC } from 'react';

export const Welcome: FC = () => (
  <div className="welcome">
    <h1 className="welcome-title">MDViewer</h1>
    <p className="welcome-subtitle">
      마크다운 파일을 열어보세요
    </p>
    <p className="welcome-hint">
      Cmd+O로 파일 열기 · Cmd+P로 최근 파일 검색
    </p>
  </div>
);
```

- [ ] **Step 6: 커밋**

```bash
git add src/renderer/hooks/useTabs.ts src/renderer/components/TabBar.tsx src/renderer/components/MarkdownView.tsx src/renderer/components/Welcome.tsx tests/renderer/hooks/useTabs.test.ts
git commit -m "feat(tabs): add tab system with TabBar, MarkdownView, Welcome

useTabs 훅으로 탭 상태 관리 (열기/닫기/전환/중복 방지).
TabBar, MarkdownView, Welcome 컴포넌트 구현."
```

---

## Task 13: App.tsx 조립 + 파일 열기 통합

**Files:**
- Modify: `src/renderer/App.tsx`
- Modify: `src/main/main.ts`

- [ ] **Step 1: App.tsx 구현**

`src/renderer/App.tsx`:

```tsx
import { type FC, useCallback, useEffect } from 'react';
import { useTheme } from './hooks/useTheme';
import { useTabs } from './hooks/useTabs';
import { TabBar } from './components/TabBar';
import { MarkdownView } from './components/MarkdownView';
import { Welcome } from './components/Welcome';
import { basename } from 'path';

export const App: FC = () => {
  useTheme();
  const { tabs, activeTabId, activeTab, openTab, closeTab, setActiveTab } = useTabs();

  const handleOpenFile = useCallback(async (filePath: string) => {
    const content = await window.api.invoke('file:read', filePath);
    const fileName = filePath.split('/').pop() ?? filePath;
    openTab(filePath, fileName, content);
    await window.api.invoke('history:add', filePath);
  }, [openTab]);

  const handleFileOpen = useCallback(async () => {
    const filePath = await window.api.invoke('file:open-dialog');
    if (filePath) {
      await handleOpenFile(filePath);
    }
  }, [handleOpenFile]);

  // Cmd+O
  useEffect(() => {
    const handleKeyDown = (e: KeyboardEvent) => {
      if (e.metaKey && e.key === 'o') {
        e.preventDefault();
        handleFileOpen();
      }
    };
    window.addEventListener('keydown', handleKeyDown);
    return () => window.removeEventListener('keydown', handleKeyDown);
  }, [handleFileOpen]);

  // 탭 전환 단축키 Cmd+1~9, Cmd+Shift+[/]
  useEffect(() => {
    const handleKeyDown = (e: KeyboardEvent) => {
      if (e.metaKey && e.key >= '1' && e.key <= '9') {
        e.preventDefault();
        const index = parseInt(e.key, 10) - 1;
        const tab = tabs[index];
        if (tab) setActiveTab(tab.id);
      }
      if (e.metaKey && e.shiftKey && e.key === '[') {
        e.preventDefault();
        const currentIndex = tabs.findIndex((t) => t.id === activeTabId);
        const prevTab = tabs[currentIndex - 1];
        if (prevTab) setActiveTab(prevTab.id);
      }
      if (e.metaKey && e.shiftKey && e.key === ']') {
        e.preventDefault();
        const currentIndex = tabs.findIndex((t) => t.id === activeTabId);
        const nextTab = tabs[currentIndex + 1];
        if (nextTab) setActiveTab(nextTab.id);
      }
    };
    window.addEventListener('keydown', handleKeyDown);
    return () => window.removeEventListener('keydown', handleKeyDown);
  }, [tabs, activeTabId, setActiveTab]);

  // Drag & Drop
  useEffect(() => {
    const handleDrop = async (e: DragEvent) => {
      e.preventDefault();
      const files = Array.from(e.dataTransfer?.files ?? []);
      const mdFiles = files.filter((f) => f.name.endsWith('.md') || f.name.endsWith('.markdown'));
      for (const file of mdFiles) {
        await handleOpenFile(file.path);
      }
    };
    const handleDragOver = (e: DragEvent) => e.preventDefault();

    window.addEventListener('drop', handleDrop);
    window.addEventListener('dragover', handleDragOver);
    return () => {
      window.removeEventListener('drop', handleDrop);
      window.removeEventListener('dragover', handleDragOver);
    };
  }, [handleOpenFile]);

  // open-file 이벤트 (File Association)
  useEffect(() => {
    const unsubscribe = window.api.on('app:open-file', (filePath: unknown) => {
      if (typeof filePath === 'string') {
        handleOpenFile(filePath);
      }
    });
    return unsubscribe;
  }, [handleOpenFile]);

  return (
    <div className="app">
      {tabs.length > 0 && (
        <TabBar
          tabs={tabs}
          activeTabId={activeTabId}
          onSelect={setActiveTab}
          onClose={closeTab}
        />
      )}
      <main className="app-content">
        {activeTab ? (
          <MarkdownView content={activeTab.content} />
        ) : (
          <Welcome />
        )}
      </main>
    </div>
  );
};
```

- [ ] **Step 2: main.ts에 open-file 이벤트 추가**

`src/main/main.ts`에 추가:

```typescript
// macOS에서 .md 파일 더블클릭 시
app.on('open-file', (event, filePath) => {
  event.preventDefault();
  const win = BrowserWindow.getAllWindows()[0];
  if (win) {
    win.webContents.send('app:open-file', filePath);
  }
});
```

- [ ] **Step 3: pnpm dev로 통합 확인**

```bash
pnpm dev
```

앱이 열리고 Cmd+O로 .md 파일을 열면 렌더링되어야 함.

- [ ] **Step 4: 커밋**

```bash
git add src/renderer/App.tsx src/main/main.ts
git commit -m "feat(app): integrate tabs, file open, drag-drop, keyboard shortcuts

App.tsx에서 탭 + 테마 + 파일 열기 통합.
Cmd+O, Drag&Drop, File Association, 탭 전환 단축키 지원."
```

---

## Task 14: 파일 이력 + Quick Open

**Files:**
- Create: `src/main/ipc/historyHandlers.ts`
- Create: `src/renderer/hooks/useQuickOpen.ts`
- Create: `src/renderer/components/QuickOpen.tsx`
- Test: `tests/renderer/hooks/useQuickOpen.test.ts`

- [ ] **Step 1: electron-store, fuse.js 설치**

```bash
pnpm add electron-store fuse.js
```

- [ ] **Step 2: 실패하는 테스트 작성**

`tests/renderer/hooks/useQuickOpen.test.ts`:

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { renderHook, act } from '@testing-library/react';
import { useQuickOpen } from '../../../src/renderer/hooks/useQuickOpen';
import type { HistoryEntry } from '../../../src/shared/types';

const mockEntries: ReadonlyArray<HistoryEntry> = [
  { filePath: '/docs/readme.md', fileName: 'readme.md', openedAt: '2026-04-03T10:00:00Z' },
  { filePath: '/docs/guide.md', fileName: 'guide.md', openedAt: '2026-04-03T09:00:00Z' },
  { filePath: '/notes/todo.md', fileName: 'todo.md', openedAt: '2026-04-03T08:00:00Z' },
];

const mockInvoke = vi.fn();
vi.stubGlobal('window', {
  ...window,
  api: { invoke: mockInvoke, on: vi.fn() },
});

describe('useQuickOpen', () => {
  beforeEach(() => {
    vi.clearAllMocks();
    mockInvoke.mockResolvedValue(mockEntries);
  });

  it('should load history entries sorted by openedAt desc', async () => {
    const { result } = renderHook(() => useQuickOpen());
    await act(async () => {
      await result.current.loadHistory();
    });
    expect(result.current.entries[0]?.fileName).toBe('readme.md');
    expect(result.current.entries[2]?.fileName).toBe('todo.md');
  });

  it('should filter entries by search query', async () => {
    const { result } = renderHook(() => useQuickOpen());
    await act(async () => {
      await result.current.loadHistory();
    });
    act(() => {
      result.current.setQuery('guide');
    });
    expect(result.current.filteredEntries).toHaveLength(1);
    expect(result.current.filteredEntries[0]?.fileName).toBe('guide.md');
  });

  it('should return all entries when query is empty', async () => {
    const { result } = renderHook(() => useQuickOpen());
    await act(async () => {
      await result.current.loadHistory();
    });
    expect(result.current.filteredEntries).toHaveLength(3);
  });
});
```

- [ ] **Step 3: 테스트 실패 확인**

```bash
pnpm test -- tests/renderer/hooks/useQuickOpen.test.ts
```

Expected: FAIL

- [ ] **Step 4: historyHandlers 구현**

`src/main/ipc/historyHandlers.ts`:

```typescript
import { ipcMain } from 'electron';
import Store from 'electron-store';
import { basename } from 'node:path';
import type { HistoryEntry } from '../../shared/types';

const store = new Store<{ history: HistoryEntry[] }>({
  defaults: { history: [] },
});

const MAX_HISTORY = 100;

export const registerHistoryHandlers = (): void => {
  ipcMain.handle('history:get', () => {
    const history = store.get('history', []);
    return [...history].sort(
      (a, b) => new Date(b.openedAt).getTime() - new Date(a.openedAt).getTime(),
    );
  });

  ipcMain.handle('history:add', (_event, filePath: string) => {
    const history = store.get('history', []);
    const filtered = history.filter((e) => e.filePath !== filePath);
    const entry: HistoryEntry = {
      filePath,
      fileName: basename(filePath),
      openedAt: new Date().toISOString(),
    };
    const updated = [entry, ...filtered].slice(0, MAX_HISTORY);
    store.set('history', updated);
  });
};
```

- [ ] **Step 5: useQuickOpen 구현**

`src/renderer/hooks/useQuickOpen.ts`:

```typescript
import { useState, useCallback, useMemo } from 'react';
import Fuse from 'fuse.js';
import type { HistoryEntry } from '../../shared/types';

export const useQuickOpen = () => {
  const [entries, setEntries] = useState<ReadonlyArray<HistoryEntry>>([]);
  const [query, setQuery] = useState('');

  const fuse = useMemo(
    () => new Fuse([...entries], { keys: ['fileName', 'filePath'], threshold: 0.4 }),
    [entries],
  );

  const loadHistory = useCallback(async () => {
    const history = await window.api.invoke('history:get');
    setEntries(history);
  }, []);

  const filteredEntries = useMemo(() => {
    if (query.trim() === '') return entries;
    return fuse.search(query).map((r) => r.item);
  }, [query, entries, fuse]);

  return { entries, filteredEntries, query, setQuery, loadHistory };
};
```

- [ ] **Step 6: 테스트 통과 확인**

```bash
pnpm test -- tests/renderer/hooks/useQuickOpen.test.ts
```

Expected: PASS

- [ ] **Step 7: QuickOpen 컴포넌트 구현**

`src/renderer/components/QuickOpen.tsx`:

```tsx
import { type FC, useEffect, useRef, useState, useCallback } from 'react';
import { useQuickOpen } from '../hooks/useQuickOpen';

type QuickOpenProps = {
  readonly isOpen: boolean;
  readonly onClose: () => void;
  readonly onSelect: (filePath: string) => void;
};

export const QuickOpen: FC<QuickOpenProps> = ({ isOpen, onClose, onSelect }) => {
  const { filteredEntries, query, setQuery, loadHistory } = useQuickOpen();
  const [selectedIndex, setSelectedIndex] = useState(0);
  const inputRef = useRef<HTMLInputElement>(null);

  useEffect(() => {
    if (isOpen) {
      loadHistory();
      setQuery('');
      setSelectedIndex(0);
      inputRef.current?.focus();
    }
  }, [isOpen, loadHistory, setQuery]);

  const handleKeyDown = useCallback(
    (e: React.KeyboardEvent) => {
      if (e.key === 'Escape') {
        onClose();
      } else if (e.key === 'ArrowDown') {
        e.preventDefault();
        setSelectedIndex((i) => Math.min(i + 1, filteredEntries.length - 1));
      } else if (e.key === 'ArrowUp') {
        e.preventDefault();
        setSelectedIndex((i) => Math.max(i - 1, 0));
      } else if (e.key === 'Enter') {
        const entry = filteredEntries[selectedIndex];
        if (entry) {
          onSelect(entry.filePath);
          onClose();
        }
      }
    },
    [filteredEntries, selectedIndex, onSelect, onClose],
  );

  if (!isOpen) return null;

  return (
    <div className="quick-open-overlay" onClick={onClose}>
      <div className="quick-open" onClick={(e) => e.stopPropagation()}>
        <input
          ref={inputRef}
          className="quick-open-input"
          placeholder="파일명으로 검색..."
          value={query}
          onChange={(e) => {
            setQuery(e.target.value);
            setSelectedIndex(0);
          }}
          onKeyDown={handleKeyDown}
        />
        <div className="quick-open-list">
          {filteredEntries.map((entry, index) => (
            <div
              key={entry.filePath}
              className={`quick-open-item ${index === selectedIndex ? 'quick-open-selected' : ''}`}
              onClick={() => {
                onSelect(entry.filePath);
                onClose();
              }}
            >
              <span className="quick-open-filename">{entry.fileName}</span>
              <span className="quick-open-filepath">{entry.filePath}</span>
            </div>
          ))}
        </div>
      </div>
    </div>
  );
};
```

- [ ] **Step 8: main.ts에 historyHandlers 등록**

`src/main/main.ts`에 추가:

```typescript
import { registerHistoryHandlers } from './ipc/historyHandlers';

app.whenReady().then(() => {
  registerThemeHandlers();
  registerFileHandlers();
  registerHistoryHandlers();
  // ...
});
```

- [ ] **Step 9: App.tsx에 Quick Open 통합**

`src/renderer/App.tsx`에 QuickOpen 상태 + Cmd+P 단축키 추가:

```typescript
const [isQuickOpenOpen, setIsQuickOpenOpen] = useState(false);

// Cmd+P 단축키 (기존 useEffect에 추가)
if (e.metaKey && e.key === 'p') {
  e.preventDefault();
  setIsQuickOpenOpen(true);
}

// JSX에 추가
<QuickOpen
  isOpen={isQuickOpenOpen}
  onClose={() => setIsQuickOpenOpen(false)}
  onSelect={handleOpenFile}
/>
```

- [ ] **Step 10: 커밋**

```bash
git add src/main/ipc/historyHandlers.ts src/renderer/hooks/useQuickOpen.ts src/renderer/components/QuickOpen.tsx src/main/main.ts src/renderer/App.tsx tests/renderer/hooks/useQuickOpen.test.ts package.json pnpm-lock.yaml
git commit -m "feat(quick-open): add Cmd+P fuzzy search for recent files

electron-store로 최근 파일 이력 관리 (최대 100개).
fuse.js 퍼지 검색, 화살표 키 탐색, Enter 선택."
```

---

## Task 15: 앱 메뉴

**Files:**
- Create: `src/main/menu.ts`
- Modify: `src/main/main.ts`

- [ ] **Step 1: menu.ts 구현**

`src/main/menu.ts`:

```typescript
import { Menu, type BrowserWindow } from 'electron';

export const createMenu = (mainWindow: BrowserWindow): Menu =>
  Menu.buildFromTemplate([
    {
      label: 'File',
      submenu: [
        {
          label: 'Open...',
          accelerator: 'Cmd+O',
          click: () => mainWindow.webContents.send('menu:open-file'),
        },
        {
          label: 'Quick Open',
          accelerator: 'Cmd+P',
          click: () => mainWindow.webContents.send('menu:quick-open'),
        },
        { type: 'separator' },
        {
          label: 'Close Tab',
          accelerator: 'Cmd+W',
          click: () => mainWindow.webContents.send('menu:close-tab'),
        },
        { type: 'separator' },
        { role: 'quit' },
      ],
    },
    {
      label: 'View',
      submenu: [
        { role: 'zoomIn' },
        { role: 'zoomOut' },
        { role: 'resetZoom' },
        { type: 'separator' },
        { role: 'togglefullscreen' },
        { type: 'separator' },
        { role: 'toggleDevTools' },
      ],
    },
    {
      label: 'Window',
      submenu: [
        { role: 'minimize' },
        { role: 'close' },
      ],
    },
  ]);
```

- [ ] **Step 2: main.ts에서 메뉴 적용**

```typescript
import { createMenu } from './menu';

// BrowserWindow 생성 후:
const menu = createMenu(mainWindow);
Menu.setApplicationMenu(menu);
```

- [ ] **Step 3: pnpm dev로 메뉴 동작 확인**

```bash
pnpm dev
```

File > Open, File > Quick Open 메뉴가 동작하면 성공.

- [ ] **Step 4: 커밋**

```bash
git add src/main/menu.ts src/main/main.ts
git commit -m "feat(menu): add application menu with File, View, Window

File: Open, Quick Open, Close Tab, Quit.
View: Zoom, Fullscreen, DevTools."
```

---

## Task 16: 마크다운 컴포넌트 CSS 스타일링

**Files:**
- Create: `src/renderer/styles/markdown.css`
- Create: `src/renderer/styles/tabs.css`
- Create: `src/renderer/styles/quick-open.css`
- Create: `src/renderer/styles/welcome.css`

- [ ] **Step 1: markdown.css 작성**

`src/renderer/styles/markdown.css`:

```css
.markdown-view {
  display: flex;
  justify-content: center;
  padding: var(--spacing-content-padding);
  overflow-y: auto;
  height: 100%;
}

.markdown-content {
  max-width: var(--spacing-content-max-width);
  width: 100%;
}

/* Headings */
.heading {
  color: var(--color-heading);
  font-weight: 600;
  margin-top: var(--spacing-heading-margin-top);
  margin-bottom: var(--spacing-heading-margin-bottom);
  line-height: 1.25;
}

.heading-1 { font-size: var(--heading-h1); padding-bottom: 0.3em; border-bottom: 1px solid var(--color-divider); }
.heading-2 { font-size: var(--heading-h2); padding-bottom: 0.3em; border-bottom: 1px solid var(--color-divider); }
.heading-3 { font-size: var(--heading-h3); }
.heading-4 { font-size: var(--heading-h4); }
.heading-5 { font-size: var(--heading-h5); }
.heading-6 { font-size: var(--heading-h6); color: var(--color-text-secondary); }

/* Paragraph */
.paragraph {
  margin-bottom: var(--spacing-paragraph);
}

/* Inline code */
.inline-code {
  font-family: var(--font-code);
  font-size: 85%;
  background-color: var(--color-code-bg);
  color: var(--color-code-text);
  padding: 0.2em 0.4em;
  border-radius: 4px;
}

/* Code block */
.code-block-wrapper {
  position: relative;
  margin-bottom: var(--spacing-paragraph);
}

.code-block-language {
  position: absolute;
  top: 8px;
  right: 12px;
  font-family: var(--font-code);
  font-size: 12px;
  color: var(--color-text-secondary);
  user-select: none;
}

.code-block {
  font-family: var(--font-code);
  font-size: var(--font-code-size);
  line-height: var(--code-line-height);
  background-color: var(--color-code-bg);
  color: var(--color-code-text);
  padding: 16px;
  border-radius: 6px;
  overflow-x: auto;
}

/* Blockquote */
.blockquote {
  border-left: 4px solid var(--color-blockquote-border);
  padding: 0 16px;
  color: var(--color-blockquote-text);
  margin-bottom: var(--spacing-paragraph);
}

/* List */
.list {
  padding-left: 2em;
  margin-bottom: var(--spacing-paragraph);
}

.list-item {
  margin-bottom: 4px;
}

/* Table */
.table-wrapper {
  overflow-x: auto;
  margin-bottom: var(--spacing-paragraph);
}

.table {
  border-collapse: collapse;
  width: 100%;
}

.table-cell {
  border: 1px solid var(--color-table-border);
  padding: 8px 12px;
}

.table-head .table-cell {
  background-color: var(--color-table-header-bg);
  font-weight: 600;
}

.table-body .table-row:nth-child(even) .table-cell {
  background-color: var(--color-table-stripe-bg);
}

/* Link */
.link {
  color: var(--color-link);
  text-decoration: none;
}

.link:hover {
  color: var(--color-link-hover);
  text-decoration: underline;
}

/* Image */
.image {
  max-width: 100%;
  height: auto;
  border-radius: 4px;
}

/* Divider */
.divider {
  border: none;
  border-top: 2px solid var(--color-divider);
  margin: 24px 0;
}

/* Checkbox */
.checkbox {
  margin-right: 8px;
}

/* Mermaid */
.mermaid-diagram {
  display: flex;
  justify-content: center;
  margin-bottom: var(--spacing-paragraph);
}

.mermaid-diagram svg {
  max-width: 100%;
}
```

- [ ] **Step 2: tabs.css 작성**

`src/renderer/styles/tabs.css`:

```css
.tab-bar {
  display: flex;
  background-color: var(--color-code-bg);
  border-bottom: 1px solid var(--color-divider);
  overflow-x: auto;
  -webkit-app-region: drag;
}

.tab-item {
  display: flex;
  align-items: center;
  padding: 8px 16px;
  font-size: 13px;
  color: var(--color-text-secondary);
  cursor: pointer;
  border-right: 1px solid var(--color-divider);
  -webkit-app-region: no-drag;
  user-select: none;
  white-space: nowrap;
}

.tab-item:hover {
  background-color: var(--color-bg);
}

.tab-active {
  background-color: var(--color-bg);
  color: var(--color-text);
}

.tab-title {
  margin-right: 8px;
}

.tab-close {
  background: none;
  border: none;
  color: var(--color-text-secondary);
  cursor: pointer;
  font-size: 16px;
  padding: 0 4px;
  line-height: 1;
  border-radius: 2px;
}

.tab-close:hover {
  background-color: var(--color-divider);
  color: var(--color-text);
}
```

- [ ] **Step 3: quick-open.css 작성**

`src/renderer/styles/quick-open.css`:

```css
.quick-open-overlay {
  position: fixed;
  inset: 0;
  background-color: rgba(0, 0, 0, 0.4);
  display: flex;
  justify-content: center;
  padding-top: 80px;
  z-index: 100;
}

.quick-open {
  background-color: var(--color-bg);
  border: 1px solid var(--color-divider);
  border-radius: 8px;
  width: 500px;
  max-height: 400px;
  display: flex;
  flex-direction: column;
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
}

.quick-open-input {
  font-family: var(--font-body);
  font-size: 14px;
  padding: 12px 16px;
  border: none;
  border-bottom: 1px solid var(--color-divider);
  background: transparent;
  color: var(--color-text);
  outline: none;
}

.quick-open-list {
  overflow-y: auto;
  flex: 1;
}

.quick-open-item {
  padding: 8px 16px;
  cursor: pointer;
  display: flex;
  flex-direction: column;
  gap: 2px;
}

.quick-open-item:hover,
.quick-open-selected {
  background-color: var(--color-selection);
}

.quick-open-filename {
  font-size: 14px;
  color: var(--color-text);
}

.quick-open-filepath {
  font-size: 12px;
  color: var(--color-text-secondary);
}
```

- [ ] **Step 4: welcome.css 작성**

`src/renderer/styles/welcome.css`:

```css
.welcome {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100%;
  gap: 12px;
  color: var(--color-text-secondary);
}

.welcome-title {
  font-size: 48px;
  font-weight: 200;
  color: var(--color-text);
  letter-spacing: -1px;
}

.welcome-subtitle {
  font-size: 18px;
}

.welcome-hint {
  font-size: 14px;
  opacity: 0.6;
}
```

- [ ] **Step 5: CSS import**

`src/renderer/index.tsx`에 추가:

```typescript
import './styles/markdown.css';
import './styles/tabs.css';
import './styles/quick-open.css';
import './styles/welcome.css';
```

- [ ] **Step 6: pnpm dev로 스타일 확인**

```bash
pnpm dev
```

파일을 열어 마크다운 렌더링이 아름답게 보이면 성공.

- [ ] **Step 7: 커밋**

```bash
git add src/renderer/styles/ src/renderer/index.tsx
git commit -m "style(ui): add CSS for markdown, tabs, quick-open, welcome

CSS Variables 기반 테마 연동 스타일링.
마크다운 타이포그래피, 탭 바, Quick Open 모달, Welcome 화면."
```

---

## Task 17: File Association (macOS)

**Files:**
- Modify: `electron-builder.yml` or `package.json` (build config)
- Modify: `src/main/main.ts`

- [ ] **Step 1: electron-builder 설정에 file association 추가**

`electron-builder.yml` (또는 `package.json`의 `build` 섹션):

```yaml
fileAssociations:
  - ext: md
    name: Markdown
    role: Viewer
    mimeType: text/markdown
  - ext: markdown
    name: Markdown
    role: Viewer
    mimeType: text/markdown
```

- [ ] **Step 2: main.ts에서 앱 시작 시 파일 인자 처리**

`src/main/main.ts`에 추가:

```typescript
// 앱 시작 시 인자로 전달된 파일 처리
const fileArg = process.argv.find((arg) => arg.endsWith('.md') || arg.endsWith('.markdown'));

app.whenReady().then(() => {
  // ... 기존 코드
  if (fileArg) {
    mainWindow.webContents.on('did-finish-load', () => {
      mainWindow.webContents.send('app:open-file', fileArg);
    });
  }
});
```

- [ ] **Step 3: 커밋**

```bash
git add electron-builder.yml src/main/main.ts
git commit -m "feat(app): add .md file association for macOS

더블클릭으로 .md 파일을 MDViewer에서 열기.
앱 시작 시 인자로 전달된 파일도 처리."
```

---

## Task 18: E2E 테스트

**Files:**
- Create: `tests/e2e/app.spec.ts`
- Modify: `package.json` (test:e2e script)

- [ ] **Step 1: Playwright 설치**

```bash
pnpm add -D @playwright/test electron
```

- [ ] **Step 2: E2E 테스트 작성**

`tests/e2e/app.spec.ts`:

```typescript
import { test, expect, _electron as electron } from '@playwright/test';

test.describe('MDViewer E2E', () => {
  test('should launch and show welcome screen', async () => {
    const app = await electron.launch({ args: ['.'] });
    const page = await app.firstWindow();

    await expect(page.locator('.welcome-title')).toHaveText('MDViewer');

    await app.close();
  });

  test('should open file via Cmd+O and render markdown', async () => {
    const app = await electron.launch({ args: ['.'] });
    const page = await app.firstWindow();

    // 파일 열기 다이얼로그는 자동화 어려우므로 IPC 직접 호출
    await page.evaluate(() => {
      window.api.invoke('file:read', './docs/PRD.md');
    });

    await app.close();
  });
});
```

- [ ] **Step 3: package.json에 test:e2e 스크립트 추가**

```json
{
  "scripts": {
    "test:e2e": "playwright test --config=tests/e2e/playwright.config.ts"
  }
}
```

- [ ] **Step 4: 커밋**

```bash
git add tests/e2e/ package.json
git commit -m "test(e2e): add Playwright E2E tests for app launch

앱 실행 + Welcome 화면 표시 E2E 테스트."
```

---

## Task 19: KaTeX CSS 번들링 + highlight.js 테마

**Files:**
- Modify: `src/renderer/styles/fonts.css`
- Modify: `src/renderer/index.tsx`

- [ ] **Step 1: KaTeX CSS import 추가**

`src/renderer/styles/fonts.css`에 추가:

```css
@import 'katex/dist/katex.min.css';
```

- [ ] **Step 2: highlight.js GitHub 테마 import**

`src/renderer/index.tsx`에 추가:

```typescript
import 'highlight.js/styles/github.css';
```

다크 모드 전용 테마는 CSS media query로 분기:

```css
/* global.css에 추가 */
@media (prefers-color-scheme: dark) {
  @import 'highlight.js/styles/github-dark.css';
}
```

- [ ] **Step 3: pnpm dev로 수식 + 코드 렌더링 확인**

수식과 syntax highlighting이 올바르게 표시되면 성공.

- [ ] **Step 4: 커밋**

```bash
git add src/renderer/styles/ src/renderer/index.tsx
git commit -m "style(rendering): bundle KaTeX CSS and highlight.js GitHub theme

수식 렌더링용 KaTeX CSS, 코드 syntax highlighting용 GitHub 테마 번들."
```

---

## Self-Review Checklist

- **Spec coverage**: F1(마크다운 렌더링) ✓, F2(파일 열기) ✓, F3(탭) ✓, F4(Quick Open) ✓, F5(링크 동작) ✓, F6(테마) ✓, F7(이미지) ✓, 보안 ✓, 수식 ✓, Mermaid ✓, File Association ✓, E2E ✓
- **Placeholder scan**: TBD/TODO 없음 ✓
- **Type consistency**: IpcChannels, HistoryEntry, Tab, TabState, Theme 전체 태스크에서 일관 ✓
